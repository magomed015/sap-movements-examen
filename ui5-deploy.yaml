# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: movementsapp1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://a03z.ucc.ovgu.de/
          client: "238"
        app:
          name: ZSD_020_MOVE
          package: ZSD_020_MOVEMENTS
          transport: A03K901665
        exclude:
          - /test/
